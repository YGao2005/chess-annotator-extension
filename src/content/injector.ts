// This script is injected into the page context (MAIN world) for WebSocket interception.
// Currently a placeholder for Phase 4 WebSocket intercept implementation.

(() => {
  // Will be used for WebSocket interception in a future phase.
  // For now, the content script relies on the game object API and DOM observation.
  console.log('[ChessAnnotation] Page context script loaded');
})();
